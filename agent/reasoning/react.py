"""[SEMANA 5 — PENDIENTE] Descomposición de tareas (laboratorios 13 y 14).

El equipo implementa aquí el patrón elegido:
  - ReAct (laboratorio 13): nodos reason/act con traza de razonamiento explícita, o
  - Plan-and-Execute (laboratorio 14): nodos planner/executor/replanner.

Requisito no negociable: una red de seguridad (max_iterations o max_steps)
que evite ciclos infinitos.
"""


def build_reasoning_graph(tools: list | None = None):
    """Compila el grafo con descomposición explícita de tareas."""
    raise NotImplementedError(
        "Semana 5: implementar ReAct (laboratorio 13) o Plan-and-Execute (laboratorio 14)."
    )
